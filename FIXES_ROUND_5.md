# إصلاحات هذه الجولة — الصور بلا شرح، وما ظهر معها

المصدر: مراجعة المخرج النهائي الذي أرسله المستخدم
(`WhatsApp Video 2026-08-30 at 10.48.24 AM.docx`).

## 1. الصورة تخرج بلا أي تعليق — العيب المُبلَّغ

**السبب الجذري (اثنان معًا):**

* `document/word_generator.py::_add_figure` كان يبني سطر «شكل N ·
  التوقيت» ثم **يتجاهل `block.caption` تجاهلًا تامًا**. أي نص تسمية
  تحسبه الخطة لم يكن يصل إلى الملف أصلًا.
* `ai/rewriter.py` لم يكن يطلب من النموذج تسمية أصلًا؛ التسمية
  المحسوبة كانت «لقطة عند 00:00:17» — توقيت لا شرح.

**العلاج:**

* عقد المخرج صار يشمل `figures: [{"t": "00:00:17", "caption": "…"}]`،
  وتُمرَّر أزمنة اللقطات الواقعة في مدى كل دفعة مع نص الدفعة نفسها،
  فيكتب النموذج تعليقًا من الكلام المصاحب للحظة اللقطة لا من فراغ.
* الأزمنة تُطبَّع (`timestamp_to_seconds` ← `seconds_to_display`) قبل
  المطابقة، فردّ `0:17` يطابق `00:00:17`.
* `_add_figure` صار يطبع سطر التسمية الوصفي تحت رقم الشكل.

## 2. كل الصور تتكدّس ثم يأتي الشرح كله في آخر القسم

`_assemble` كان يضيف كل الصور ثم كل الفقرات. صار يجمع الاثنين في
قائمة أحداث واحدة ويرتّبها زمنيًا (الصورة تسبق النص عند تساوي الزمن:
المتحدّث يعرض الشاشة ثم يشرحها).

وليكون للفقرة زمن أصلًا: `_paragraph_times` يشتقّ توقيت كل فقرة
مُعاد صياغتها من توقيت المقاطع المصدر بنسبة الأحرف — **باستيفاء
خطّي داخل المقطع**، لا بالقفز إلى بدايته. الاستيفاء ضروري: دفعة
مبنية على مقطع واحد كانت تمنح فقراتها الثلاث نفس الزمن فتتكدّس
الصور بعدها من جديد.

## 3. «عدد الأقسام: 1» لفيديو كامل

حجم الدفعة الثابت (3500 حرفًا) يبتلع تفريغ فيديو قصير كاملًا، فيخرج
المستند بقسم واحد عنوانه عنوان الوثيقة — بلا بنية ولا فهرس مفيد.

صار الحجم مشتقًّا: `min(الإعداد, max(700, إجمالي الأحرف ÷ 5))`.
ترتيب الحدّين مقصود: **الإعداد المُصرّح به يبقى سقفًا دائمًا** (تحكّم
المستخدم في الكلفة)، والاشتقاق يخفضه فقط ولا ينزل تحت حدٍّ مفيد.

## 4. «ai:anthropic/default» بدل اسم النموذج

`generated_by` صار يقرأ `provider.model` فعليًا:
`ai:anthropic/claude-sonnet-5`.

## 5. سطرا تلميح مكرّران فوق الفهرس

بقي سطر واحد: «اضغط داخل هذا الإطار ثم F9 لتحديث الفهرس».

## تغطية الانحدار

سبع حالات جديدة في `tests/test_rewrite_pipeline.py`: تداخل الصور مع
النص، الترتيب الزمني داخل كل قسم، تعدّد الأقسام للفيديو القصير،
السقف المُصرّح به لحجم الدفعة، اسم النموذج الحقيقي، وصول تعليق
النموذج إلى كتلة الصورة، وتوزّع أزمنة الفقرات داخل مقطع واحد.

المجموع: **230 اختبارًا ناجحًا**.
