# خطوة واحدة متبقية عليك: تحرير مستودع Git

## ما حدث

أُنشئ المستودع وسُجّلت فيه **لقطة الحالة الأصلية قبل أي تعديل**
(‏commit واحد: «الحالة الأولية قبل جولة الإصلاحات»)، فنقطة الرجوع محفوظة.

لكن بيئة العمل التي شغّلتُ منها Git لا تملك صلاحية **حذف** الملفات على
هذا المجلد، وGit يحذف ملفات قفل مؤقتة بعد كل عملية. فبقيت ثلاثة ملفات قفل
تمنع أي أمر Git لاحق.

## الإصلاح — دقيقة واحدة في PowerShell

```powershell
cd "C:\Users\IBM\Desktop\Video Converter DOC\VideoToArabicWord"

# 1) احذف ملفات القفل العالقة
Remove-Item .git\index.lock, .git\HEAD.lock, .git\objects\maintenance.lock -ErrorAction SilentlyContinue
Get-ChildItem .git\objects -Recurse -Filter "tmp_obj_*" | Remove-Item -Force

# 2) تأكّد أن المستودع سليم
git status
git log --oneline        # يجب أن يظهر: الحالة الأولية قبل جولة الإصلاحات

# 3) راجع كل ما تغيّر
git diff --stat
git diff                 # للمراجعة التفصيلية

# 4) سجّل الجولة
git add -A
git commit -m "جولة الإصلاحات السادسة: أعطال حرجة + ميزات + بنية تحتية

- منع التنزيل الصامت للنموذج (ADR-014)
- مسارات الكتابة المؤقتة خارج مجلد المستخدم (البند 29)
- لا أثر على القرص عند استيراد السجلّ
- وصل محرّكات ASR بالإعداد والواجهة (ADR-017)
- اختبار المفتاح على خيط خلفي، حفظ الإعداد، مجلد إخراج
- عزل فشل الصياغة عند الدفعة + إعادة محاولة
- ملفات ترجمة SRT/VTT، إعادة بناء المستند، فهرس الأشكال
- calibrate_scenes.py و build.spec و CI و pyproject
- 253 اختبارًا · 661/661 فحص قبول · ruff نظيف"
```

## للتراجع عن كل شيء

```powershell
git reset --hard HEAD      # يعيد كل شيء إلى ما قبل الجولة
```

## تنظيف

مجلد `_to_delete/` في مجلد التسليم يحوي ملفات نقل مؤقتة استعملتُها —
احذفه. وكذلك حزمة `matching/` الفارغة داخل المشروع (لا وحدة تستوردها).

بعد ذلك يمكنك رفع المشروع إلى GitHub وسيعمل سير عمل CI تلقائيًا:

```powershell
git remote add origin <رابط المستودع>
git push -u origin master
```
