
## 1.4.0 — النجاة: استئنافٌ داخل التفريغ (7 سبتمبر 2026)

بعد أن أثبت القياس أن السرعة محدودة بالعتاد، صار السؤال الأصحّ ليس
«كم نسرّع» بل «ماذا يحدث إن انقطع». والجواب كان: **يضيع كلّ شيء**.

الاستئناف كان على مستوى المرحلة لا داخلها. والتفريغ مرحلةٌ واحدة تمتدّ
أربع ساعات ونصف على محاضرة ثلاث ساعات — فانقطاع كهرباء أو إعادة تشغيل
ويندوز في الساعة الرابعة يبدأ من الصفر.

* `TranscriptionCheckpoint`: المقاطع والكلمات و`resume_at`، تُكتب كتابةً
  ذرّية كل **دقيقة من زمن المعالجة**. الحدّ على زمن المعالجة لا على
  زمن الصوت لأن ما يخسره المستخدم يُقاس بالدقائق الضائعة. أسوأ خسارة
  ممكنة: دقيقة واحدة.
* الاستئناف يقصّ الصوت عند `resume_at` بالضبط. الملف PCM، فـ`-ss` عليه
  قصٌّ دقيق لا بحثٌ إلى إطار مفتاحي — فلا يتكرّر مقطع ولا يسقط
  (‏ADR-010).
* ما يُحفظ **خامٌ قبل إعادة التشكيل**. الطيّ والقسمة يُطبَّقان على
  القائمة المجموعة في النهاية، وإلا فاتت حلقةُ تكرارٍ تقع على حدّ
  الاستئناف بالضبط — وهي الحالة الشائعة، لأن الانقطاع يقع في الصمت.
* التقدّم يُحسب على الصوت الكامل: استئنافٌ عند 80٪ يبدأ الشريط من 80٪.
  شريطٌ يعود إلى الصفر بعد أربع ساعات يقول للمستخدم إن عمله ضاع.
* `supports_resume` رايةٌ صريحة على المحرّك. المحرّك الذي لا يعلنها لا
  تُكتب له نقاط حفظ أصلًا: كتابة نقاطٍ لا يقرؤها أحد تُوهم بأمانٍ لا
  وجود له. ومَن لم يُحدَّث توقيعه لا ينكسر — الوسيطان يُمرَّران بالاسم
  ولمن أعلن فقط.

**والخطر في هذه الميزة ليس ألّا تستأنف، بل أن تستأنف خطأً**: توقيتٌ
مُزاح يُطابق الصور على لحظات لا تطابق الكلام، وهو عطلٌ صامت لا يرفع
استثناءً ويُفسد المستند كلّه. فأكثر الاختبارات التسعة عشر حرّاسٌ على
ذلك: إزاحة المقاطع، إزاحة توقيت الكلمات، تفرّد المعرّفات عبر الحدّ،
الطيّ فوق الحدّ، وبصمةٌ مختلفة تعني مقاطع من إعدادٍ آخر فتُرفض.

ونقطة الحفظ تُحذف عند النجاح: بقاؤها بعده يعني استئنافًا من منتصف عملٍ
تامّ. وفشل كتابتها لا يُسقط تفريغًا جاريًا منذ ساعات — أسوأ ما يُفقد
هو الاستئناف نفسه.

**التحقق:** 652 اختبارًا ناجحًا / صفر فشل (كانت 633).

## 1.4.0 — قياسُ ما لم أبنِه: المرحلتان البصريّتان (7 سبتمبر 2026)

على محاضرة حقيقية مدّتها 8:15 بدل تسجيل الـ88 ثانية:

    كشف المشاهد   33.4 ث   73 مشهدًا              RTF 0.067
    اللقطات       39.4 ث   26 صورة من 365 مرشّحًا   RTF 0.079
    المجموع       72.8 ث                          RTF 0.147

أي **26 دقيقة على محاضرة ثلاث ساعات** على آلة القياس، وأكثر على جهازك.
واللقطات تكلّف 108 ملّي ثانية للمرشّح الواحد لأنها تقفز قفزًا عشوائيًا:
كل قفزة تُجبر المفكِّك على البدء من أقرب إطار مفتاحي قبلها.

**وقرّرتُ ألّا أبني الدمج.** أفضل ما يعطيه — نقل تقييم الصور إلى
التمريرة التي تفكّ الإطارات أصلًا، ثم استخراج الفائزين وحدهم — يوفّر
نحو 40٪ من المرحلتين، أي **6٪ من الزمن الكلّي**. وثمنه أن تُقيَّم حدّة
الصورة على نسخة بعرض 480 بكسل بدل الأصل، وحدّة الصورة تتبع المقياس.
مخاطرةٌ في الجزء الذي يراه المستخدم بعينه، مقابل ستّة بالمئة.

وكذلك **تشغيل التفريغ والمسار البصري معًا**: لا مكسب فيه على جهاز
مشبَّع. مجموع العمل واحد، والمكسب الوحيد ملء أنوية فارغة — وهي تمتلئ
أصلًا بعد إصلاح عدد الخيوط. أُسقط للسبب نفسه.

`tools/bench_visual.py` يبقى ليُعاد القياس إن تغيّر شيء.

**الصورة الكاملة للسرعة بعد القياس كلّه:** التفريغ 85٪ ومحدود بمعالج لا
يملك `avx512_vnni`؛ الخيوط 4 ← 7 مكسبٌ مجّاني لم يُقَس على جهازك بعد؛
الجشع ×1.24 باختيارك؛ والبصري 15٪ لا يستحقّ المخاطرة. فما بقي ليس
تسريعًا بل **نجاة**: تشغيلٌ يمتدّ أربع ساعات ونصف يجب أن يحتمل الإغلاق.

## 1.4.0 — أين يذهب الوقت فعلًا (7 سبتمبر 2026)

بدأتُ بالقياس لا بالتحسين، والمصدر سجلّك أنت على تسجيل 88 ثانية:

    التحقّق           0.2 ث
    البيانات          0.1 ث
    استخراج الصوت     4.7 ث
    **التفريغ       158.9 ث**   ← 85٪ من الزمن
    كشف المشاهد      10.7 ث
    اللقطات          11.2 ث
    المطابقة          0.1 ث
    المستند           0.5 ث

فكل تحسين خارج التفريغ يتنازع على 15٪. وتشغيلٌ ثانٍ أعطى النسبة نفسها.

**١ · جهازك يشتغل بنصف خيوطه.** السجلّ يقول:

    تحميل Whisper (large-v3-turbo) على cpu/int8 · خيوط=4 · beam=5

على `Ryzen 5 PRO 3400GE` — أربع أنوية وثمانية خيوط. والافتراضي في
الشيفرة صار `0` (اشتقاق من عدد الأنوية ← 7) منذ دفعة `582b812`، ومعه
تعليقٌ يقول بالحرف إن «الرقم الثابت 4 كان يترك نصف الأداء». لكنّ
`~/.config/video_ai_doc/config.yaml` عندك يحمل `cpu_threads: 4` من قبل
ذلك التغيير — **والمحفوظ يفوز على الافتراضي**.

أي أن كل ضبطٍ أحسّنته يصل إلى المستخدم الجديد وحده. وهذا صنف عطل لا
يظهر في أي اختبار يبني الإعداد من الافتراضيات — وكلّ اختباراتنا كانت
كذلك.

* `SUPERSEDED_DEFAULTS` + `drop_superseded_defaults()`: قيمةٌ محفوظة
  تساوي افتراضًا **سابقًا بعينه** تُسقَط فيعود التلقائي. والحدّ ضيّق
  عمدًا: أي قيمة أخرى اختيارُ المستخدم ولا تُمسّ، والتغيير يُسجَّل.
* `tests/test_config_migration.py` — عشرة اختبارات، منها حارسٌ على
  الجدول نفسه وحارسٌ على أن اختيار المستخدم لا يُمسّ.

**٢ · «الجشع أسرع بمرّتين» كان رقمًا مختلَقًا.** التعليق في
`config/settings.py` قال «مرّة ونصف إلى مرّتين… ساعة ونصف مقابل ثلاث
ساعات»، وحاسبة التقدير كانت تقسم على `1.7`. لم يُقَس شيء من ذلك.

القياس (`tools/bench_beam.py`، دقيقتان من شرح عربي حقيقي،
`large-v3-turbo/int8`، ثلاث تكرارات، والمحرّك محمَّل قبل التوقيت):

    beam=5   وسيط 83.1 ث   تشتّت 4.0 ث   RTF 0.692
    beam=1   وسيط 66.8 ث   تشتّت 2.5 ث   RTF 0.557   ×1.24

**×1.24 لا ×2.** أي أن ثلاث ساعات محاضرة تصير 4.4 ساعة بدل 5.5، لا
2.75. وفرق النصّ 3.7٪ من الكلمات — كلمة من كل 27 — ولا مرجع مصحَّح
يقول أيّهما أصحّ. فالافتراضي يبقى على البحث الشعاعي، والخيار معروض
بالرقم الصحيح لا بوعدٍ مضاعف.

* `GREEDY_SPEEDUP = 1.24` مصدرًا واحدًا: القائمة والتلميح وحاسبة
  التقدير. كان التقدير المعروض أقصر من الواقع بنحو الثلث عند اختيار
  الجشع.

**٣ · مفتاحٌ فحصتُه فوجدته مغلقًا.** `word_timestamps` تُتّهم عادةً
بربع زمن الفكّ. القياس: 79.1 ث مُفعَّلة مقابل 77.2 ث معطَّلة — **×1.02**،
أي لا مكسب. والمعامل حامل لوزنٍ (مطابقة الصور، وكبح الهلوسة فوق
الصمت). أُغلق الباب بالقياس لا بالرأي.

**٤ · حدُّ العتاد، ورقمٌ لا يقبل التحسين البرمجي.** نفس النموذج ونفس
الإعداد يعطي RTF 0.69 على آلة القياس و~1.6 على جهازك — 2.3×. السبب
مقروء من راية المعالج: آلة القياس تملك `avx512_vnni`، وجهازك يملك
`avx2` وحده. و`ctranslate2` يستعمل نوى VNNI لحساب int8. هذه خاصّية
عتاد لا يغلقها كود.

**٥ · تصحيح ادّعاء سابق منّي.** قلتُ إن التفريغ غير حتمي بناءً على
تشغيلين أعطيا WER 38.8٪ و34.7٪. `self_divergence` في المقياس يقارن
تشغيلين بنفس الإعداد بالضبط: خرج **0.0٪** في الأربعة كلّها. المخرج
حتمي، والاختلاف الذي رأيتُه يومها كان في شيء آخر لم أضبطه.

**ما لم يُنجَز بعد**، وترتيبه من القياس لا من الحدس: مرحلتا المشاهد
واللقطات تقرآن الفيديو مرّتين (15٪ من الزمن، واللقطات تقفز عشوائيًا
بدل تمريرة متسلسلة)، ونقطة حفظ داخل التفريغ — اليوم إغلاقٌ في الساعة
الرابعة من خمس يُضيع الخمس كلّها.

## 1.4.0 — ثلاثة أعطال رآها المستخدم ولم يره أي مقياس (7 سبتمبر 2026)

الثلاثة من صنعي، وكلّها في عملٍ سلّمتُه على أنه جاهز.

**١ · المثبِّت لم يخرج، وقلتُ إنه خرج.**
خطوة «تأكيد أن غياب ffmpeg من PATH لا يمنع الإقلاع» كانت تُفرّغ `PATH`
لتحاكي جهاز معلّم نظيف — فتُخفي ffmpeg **وتُخفي المفسّر نفسه**:

    The term 'python' is not recognized as a name of a cmdlet…

فسقط البناء ثلاث مرّات متتالية، ولم يُنتَج المثبِّت قطّ. وكنتُ قد قلتُ
«الـexe جاهز بالفعل — ينقصه وسم»؛ رأيتُ الملفّات في المستودع ولم أفحص
نتيجة تشغيل واحد.

* نلتقط `(Get-Command python).Source` **قبل** التفريغ، ثم نستدعي المفسّر
  بمساره المطلق.
* وحارس جديد: إن بقي ffmpeg في `PATH` تسقط الخطوة صراحةً. بلا هذا تصير
  الخطوة تمرّ على حزمة معطوبة — وهو العطل الأصلي الذي وُجدت لتمنعه.

**٢ · زرّ «اقترح مصطلحات من الشاشة» لم يعمل ولا مرّة واحدة.**
`extract_video_facts` تأخذ خرج ffprobe (قاموسًا) وتُعيد قاموسًا، وكُتب
لها استدعاءٌ يمرّر المسار ويقرأ خاصّية:

    extract_video_facts(self.video_path).duration_seconds
    ← 'WindowsPath' object has no attribute 'get'

والاستدعاء الصحيح كان على بُعد مئتي سطر في `_refresh_estimate`. تعريفان
لشيء واحد يفترقان بصمت — الدرس نفسه الذي أنتج `document/titles.py`.

* `_source_duration_seconds()` واحدة تستعملها الدالّتان.
* حارسٌ على الملف الصوتي: مسح أربعة عشر إطارًا سوداء ثم قول «لم يُعثر
  على مصطلحات» جوابٌ صحيح لسؤال خاطئ.
* `tests/test_screen_terms_button.py` — ثمانية اختبارات تضغط الزرّ فعلًا
  بدل استدعاء دالّة مساعدة.

**٣ · الشعار كان يُرسم بلون أرضيّته.**
شريط الواجهة العلوي أرضيّته `theme.INK`، والشعار مرسوم بـ`theme.INK`
نفسه — تباين **1:1**. فلم يظهر منه إلا مثلّث التشغيل باهتًا. وعطلٌ ثانٍ
تحته: `Pillow` ترسم حدّ الدائرة **إلى الداخل**، فسُمك 12 يترك نصف قطر
داخلي 10 بينما رأس المثلّث على 14 — يخترق الحلقة فيصير الشكل بقعةً لا
حرفًا.

* `assets/logo_light.png`: نسخة فاتحة من **نفس** دالّة الرسم (وسيطا لون،
  لا رسمٌ ثانٍ يفترق عند أوّل تعديل). التباين مقيس على `INK`:
  ‏14.0:1 للحبر و9.9:1 للمثلّث.
* هندسة الشكل صارت ثوابت مسمّاة، والعلاقة بين السُمك ونصف القطر الداخلي
  يحرسها اختبار حسابي.
* الـSVG صار يطابق ما يُرسم فعلًا: حدّ SVG على منتصف المسار وحدّ Pillow
  إلى الداخل، فكان الملفّان يصفان شكلين مختلفين.
* `build.spec` كان يشحن `logo.png` وحده — أي شعارٌ يظهر من المصدر
  ويختفي في الحزمة.

**٤ · وسائط المستخدم دخلت المستودع.**
الدفعة `f990928` ضمّت 67 ميغابايت من تسجيلات المحاضرات — ملفّات
المستخدم، فيها أصوات زملاء، لا مصدر البرنامج. أُخرجت من التتبّع وأُضيفت
امتدادات الوسائط إلى `.gitignore`. (الملفّات باقية على القرص؛ وإخراجها
من **تاريخ** المستودع يحتاج إعادة كتابة ودفعًا قسريًّا — قرارٌ للمستخدم.)

**التحقق:** 622 اختبارًا ناجحًا / صفر فشل (كانت 607).

## 1.4.0 — آخر أربعة أعطال على ويندوز (3 سبتمبر 2026)

بعد إصلاحَي الجولتين السابقتين وصل CI على ويندوز إلى **حزمة الاختبارات
نفسها لأول مرة**: 384 ناجحًا، 4 ساقطة. الأربعة كلها افتراضات لينكس
مبثوثة في الاختبارات لا في الكود.

**١ · العملية الابنة ترث cp1252.**
`test_engine_probe_is_import_free` يشغّل شيفرة في عملية منفصلة ثم يطبع
رسالة الخطأ العربية — فتنهار العملية الابنة بـ `UnicodeEncodeError`
عند طباعة الرسالة **التي يفترض الاختبار أن يتحقّق منها**. الإصلاح في
نقاط الدخول لا يصل إلى عملية تُشغَّل بـ ``python -c``.

* `_in_clean_process` صار يُقدّم `_BOOTSTRAP` لكل شيفرة: جذر المشروع في
  المسار ثم `enable_utf8_console()` — تمامًا كما تفعل نقطة دخول حقيقية،
  فالعملية الابنة تختبر السلوك الفعلي لا سلوكًا مصطنعًا.
* `subprocess.run` صار يفكّ المخرجات بـ UTF-8 صراحةً بدل ترميز المنطقة.

**٢ · ثلاثة اختبارات تفترض غياب مخزن أسرار النظام.**
`_load_or_create_key` يفضّل `keyring` ولا يكتب ملف مفتاح إطلاقًا حين
ينجح. على لينكس في CI لا يوجد backend فيقع الاحتياط الملفّي وتمرّ
الاختبارات؛ على ويندوز يعمل Credential Manager فلا يُنشأ
`.secret.key` — فسقطت ثلاثة اختبارات تفترض وجوده.

الافتراض كان **خطأ الاختبارات لا خطأ الكود**: عدم كتابة مفتاح على القرص
حين ينجح مخزن النظام هو السلوك الصحيح المقصود.

* `no_keyring` fixture يُجبر مسار الاحتياط صراحةً في الاختبارات التي
  تخصّه، فتصير حتمية على كل نظام.
* `test_os_credential_store_is_preferred_over_a_key_file` جديد: المسار
  المفضَّل — وهو الوحيد العامل فعلًا على أجهزة المستخدمين — كان **بلا
  تغطية إطلاقًا**. يؤكّد وصول المفتاح إلى المخزن، و**عدم** ظهور ملف
  مفتاح على القرص: وجوده يعني تسريبه إلى نسخة احتياطية أو مزامنة
  سحابية، وهو بالضبط ما وُجد المخزن لتفاديه.
* `test_fallback_round_trips_without_any_keyring` يغطّي جهازًا بلا مخزن.

**التحقق:** 398 اختبارًا ناجحًا / صفر فشل.

## 1.4.0 — تخضير CI بالكامل + تسرّب البصمة إلى الاسم المعروض (3 سبتمبر 2026)

جولة ثانية على أعطال CI. الأولى أصلحت doctor.py، فانكشف ما خلفه.

**١ · الترميز لم يكن عطل ملف واحد بل صنف عطل.**
بعد إصلاح `doctor.py` مرّت خطوة «فحص البيئة» على ويندوز، فانتقل
`UnicodeEncodeError` نفسه إلى الخطوة التالية:
`tools/make_test_corpus.py`. تغطية ثلاث نقاط دخول كانت تضييقًا خاطئًا —
**كل** أداة هنا تطبع عربية.

* `enable_utf8_console()` صار يُستدعى في كتلة `__main__` لكل أدوات
  `tools/` الإحدى عشرة (الأدوات التي لا تضيف جذر المشروع إلى `sys.path`
  تضيفه في الكتلة نفسها قبل الاستيراد).
* `tests/test_console_encoding.py`: اختبار مُعامَل يمرّ على كل نقطة دخول
  ويؤكّد وجود الاستدعاء — أداة جديدة تُولد بالثغرة نفسها تسقط هنا لا
  عند المستخدم.

**٢ · حزم Qt أسقطت الوظيفة على ubuntu 24.04.**
`libxcb-xinerama4` أُزيلت من noble، فسقط `apt-get` برمز 100.
القائمة اختُصرت إلى ما تحتاجه منصّة `offscreen` فعلًا:
`libegl1 libgl1 libxkbcommon0 libdbus-1-3` — حزم `xcb-*` بلا معنى بلا
خادم عرض.

**٣ · البصمة تسرّبت إلى كل اسم معروض.**
`source_fingerprint` تُعيد **20** حرفًا سداسيًا عشريًا، بينما
`strip_source_fingerprint` كانت تطابق `__[0-9a-f]{8}` — طول قديم مكتوب
بخط اليد لم يُحدَّث حين أُضيف `utils/fingerprints.py`. التنظيف توقّف
صامتًا، فظهرت أسماء مثل `Lecture__46b8dd1239bce8b8c7db` في قائمة المهام
وفي عناوين المستندات المدموجة.

* `FINGERPRINT_LENGTH` في `utils/fingerprints.py` صار المصدر الوحيد
  للطول، ويُقرأ منه في `strip_source_fingerprint` — لا يمكن أن يفترقا
  ثانيةً. الحدّ الأدنى 8 يُبقي مهامًّ أُنشئت بالطول السابق قابلة للتنظيف.
* اختباران في `tests/test_fingerprints.py` يولّدان بصمة **حقيقية** بدل
  قيمة ثابتة، فتغيّر الطول يسقط الاختبار لا المستخدم.

**التحقق:** حزمة الاختبارات الكاملة **396 ناجحًا / صفر فشل**، وبوابات
القبول **803/803 (100%)** على بيئة مطابقة لبيئة CI (لينكس، Qt offscreen،
مصفوفة الفيديو مولَّدة).

## 1.4.0 — إصلاح عاجل: انهيار doctor.py على طرفية ويندوز الإنجليزية (3 سبتمبر 2026)

**البلاغ:** أول تشغيل لـ CI بعد رفع المشروع سقط بالكامل — أربع وظائف
اختبار ووظيفة الأسلوب.

**العطل الأول (ويندوز · حقيقي يمس المستخدم):**
`python tools/doctor.py` ينهار بـ
`UnicodeEncodeError: 'charmap' codec can't encode characters` قبل أن
يطبع سطرًا واحدًا. السبب أن رسائل الأداة عربية، و‏Python على ويندوز
يأخذ ترميز الطرفية من صفحة الترميز النشطة — وهي `cp1252` على الإعداد
الإنجليزي الافتراضي ولا تحوي حرفًا عربيًا. هذا ليس عطل CI: الأمر نفسه
هو الخطوة الثالثة في README و‏INSTALL، فكان يصطدم به أي مستخدم ويندوز
لم يغيّر صفحة الترميز — من الطبقة نفسها التي جاء منها إصلاح R3.

* `utils/console.py` جديد: `enable_utf8_console()` يعيد ضبط
  stdout/stderr إلى UTF-8 بـ `errors="replace"` (طرفية قديمة لا تعرض
  العربية أفضل من أداة تنهار)، ويتجاهل بصمت المجاري التي لا تملك
  `reconfigure`.
* يُستدعى في أول `main()` لنقاط الدخول التي يشغّلها المستخدم فعلًا:
  `app.py`، `tools/doctor.py`، `tools/run_pipeline.py`.
* **قرار:** لم يُضبط `PYTHONIOENCODING` في CI رغم أنه كان سيُخضّر
  الوظائف فورًا — ذلك يخفي العطل عن المستخدم بدل إصلاحه، ويجرّد CI من
  قدرته على منع تكراره.
* `tests/test_console_encoding.py`: يبني مجرى `cp1252` حقيقيًا، ويؤكّد
  أولًا أن الطباعة تنهار بلا الإصلاح (وإلا فالاختبار بلا معنى) ثم أنها
  تنجح بعده.

**العطل الثاني (لينكس · جمع الاختبارات):**
`tests/test_ui_layout.py` أسقط **حزمة الاختبارات كلها** بـ
`ImportError: libEGL.so.1` على متتبّع ubuntu العاري. الحارس كان
`importorskip("PyQt6")` وهو لا يكفي: `import PyQt6` ينجح بلا أي مكتبة
نظام، والانهيار يقع عند `QtWidgets`.

* الحارس صار على الوحدة الفعلية `PyQt6.QtWidgets`، وضبط
  `QT_QPA_PLATFORM=offscreen` سبقه.
* CI يثبّت مكتبات Qt على لينكس (`libegl1`, `libgl1`,
  `libxkbcommon-x11-0`, `libxcb-*`) — فالتخطّي صار للأجهزة العارية لا
  لإخفاء عطل: اختبار تخطيط الواجهة يعمل فعلًا في CI، وهو سبب وجوده.

**العطل الثالث (الأسلوب):** 17 مخالفة ruff. عشر أُصلحت آليًا (ترتيب
استيرادات، فراغات، استيراد غير مستخدم في `document/quality.py`)، وسبع
`E701` في `tools/build_smoke.py` أُعيدت كتابتها بجُمَل مستقلة.

## 1.4.0 — إصلاح عاجل: التطبيق لا يُقلع على ويندوز (3 سبتمبر 2026)

**البلاغ:** `python app.py` يتوقف فورًا برسالة
«1 مشكلة / 1 problem(s) blocking startup»، وكتلة «COPY AND RUN THESE
COMMANDS» تعرض جملة عربية بدل أمر.

**السبب الجذري — ثلاثة عيوب في `check_write_access` (جولة R3):**

1. **حذف ملف مفتوح.** `tempfile.mkstemp` تُعيد **واصفًا مفتوحًا**، والكود
   كان يحذف الملف *قبل* `os.close(fd)`. لينكس وmacOS يسمحان بحذف ملف
   مفتوح؛ **ويندوز يرفضه** بـ `PermissionError: [WinError 32]`. النتيجة:
   الفحص يفشل على **كل** جهاز ويندوز، و`require_ready()` يمنع إقلاع
   التطبيق كليًا — بينما تمرّ كل الاختبارات على لينكس. هذا بالضبط سبب
   عدم التقاط العطل قبل التسليم.
2. **مجلد مخترع.** الفحص كان يستهدف `~/VideoToArabicWord` — مجلد لا
   يكتب فيه التطبيق شيئًا (مجلد الإخراج الفعلي `~/VideoToDocOutput`) —
   **ويُنشئه** كأثر جانبي في مجلد المستخدم المنزلي.
3. **نصيحة عربية داخل كتلة الأوامر.** `fix_commands` كانت تُصفّي
   بقائمة بادئات («يُنصح»، «ضع»، «الحد»)، فتسرّبت «اختر مجلد إخراج…»
   و«وفّر 2.0 GB…» إلى الكتلة المخصّصة للنسخ واللصق.

**الإصلاح:**
* الإغلاق قبل الحذف، وتعذُّر الحذف وحده (مضاد فيروسات، مزامنة سحابية)
  لم يعد يُسقط الفحص: الكتابة نجحت فعلًا، وبقاء ملف فحص ليس فشل صلاحية.
* `default_output_dir()` يقرأ مجلد الإخراج من الإعداد الفعلي (باستيراد
  كسول محاط بـ `try` — فحص البيئة يسبق التحقق من pydantic عمدًا).
* `_is_shell_command()` تحلّ محل قائمة البادئات: الفحص على أول حرف لا
  على بادئة بعينها، ولا يشترط ASCII كاملًا كي لا يختفي الأمر على جهاز
  اسم مستخدمه عربي.
* أربعة اختبارات انحدار في `tests/test_environment_doctor.py`، منها
  اختبار يثبّت **ترتيب** الإغلاق قبل الحذف — فيمسك العطل من لينكس رغم
  أنه لا يظهر إلا على ويندوز (تُحقّق من فشله على الكود القديم).


## 1.4.0 — مراجعة: بوابة الجودة والرسائل غير المصنَّفة (3 سبتمبر 2026)

**العطل:** بوابة الجودة في `document/quality.py` (جولة P1) كانت ترفع
`AssertionError` عارية بدل أحد أخطاء التطبيق. النتيجة: فشل مشروع —
فيديو محاضرة بجودة مصدر منخفضة يهبط بدرجة الخطة عن 85% — كان يظهر
للمستخدم كـ«AssertionError: ...» بدل رسالة مصنَّفة، ولا يُسجَّل برمز
`error_code` رغم أن كل هذا البناء (`core/exceptions.py`،
`utils/error_reporting.py`) أُضيف في جولة R3 لتصنيف الأخطاء آليًا —
لكن `describe_error()` لم تكن مستدعاة من أي مكان في الواجهة أو سطر
الأوامر أصلًا (نفس نمط «مكتوب ومختبَر ولا يُستدعى» المتكرر في هذا
المشروع).

**الإصلاح:**
* `QualityGateError` جديد في `core/exceptions.py` (`E8002`) — يرث من
  `DocumentGenerationError` و`AssertionError` معًا، فيبقى متوافقًا مع
  أي كود قائم يلتقط `AssertionError` (بما فيه `tests/test_quality_gate.py`)
  بينما يشارك الآن في نظام `error_code`/`category`.
* `assert_quality_gate` يرفع `QualityGateError` بدل `AssertionError`.
* دالة جديدة `format_error_for_user()` في `utils/error_reporting.py`:
  تعرض `[code] رسالة` لأخطاء التطبيق المصنَّفة، وتترك أي استثناء آخر
  كما هو (`str(exc)`) — فلا تُفقد تفاصيل عطل برمجي غير متوقع خلف رسالة
  داخلية عامة.
* وُصلت في `ui/worker.py` (BatchWorker وPipelineWorker) و
  `tools/run_pipeline.py` (المعالجة الرئيسية، `--rebuild`، `--merge`)
  بدل `f"{type(exc).__name__}: {exc}"` أو `str(exc)` المباشرة.
* اختباران جديدان في `tests/test_release_hardening.py` يثبّتان كلا
  السلوكين.

**لم يُعالَج:** `ProviderTestWorker` (اختبار مفتاح مزوّد الذكاء
الاصطناعي) تُرك بلا تغيير عمدًا — استثناءاته من مجال شبكة/API خارجي لا
من تصنيف التطبيق، ونصّها الحالي واضح بذاته.


## 1.4.0 — Security & Release Hardening

- استخدام مخزن أسرار نظام التشغيل عبر `keyring` عند توفره مع fallback محلي متوافق.
- تدوير `app.log` تلقائيًا (10MB × 5) لمنع نمو السجل بلا حد.
- تنقية الرموز والمفاتيح الشائعة من السجل قبل الكتابة.
- حفظ `config.yaml` بطريقة ذرّية لتقليل خطر ملف إعداد نصف مكتوب.
- إضافة اختبارات أمن السجل والتدوير.
- توثيق أن `requirements.lock.txt` يُولّد على بيئة البناء نفسها.

# سجلّ التغييرات

## 1.4.0 — 1 سبتمبر 2026 · OCR: نص الشاشة تحت كل صورة

**الفكرة:** كاشف المشاهد ومنتقي الصور يلتقطان اللقطة، لكن ما هو مكتوب
فعليًا على الشاشة (عنوان، جدول، تعريف، معادلة) لم يكن يصل إلى المستند
إطلاقًا — التفريغ الصوتي وحده لا يعرفه إن لم يقرأه المحاضر بصوت عالٍ.

**الإضافة:** `video/ocr.py` — يستخرج نص كل لقطة *مختارة بالفعل* (لا كل
إطار) عبر **Tesseract**، ويُدرجه في فقرة منفصلة تحت الصورة في المستند:
«النص الظاهر على الشاشة: …».

* **قرار المحرّك:** Tesseract لا EasyOCR/PaddleOCR (يجرّان PyTorch،
  يخرقان ADR-012 ويعيدان بالضبط تضارب OpenMP الذي أُصلح في 1.3.2) ولا
  خدمة سحابية (تخرق ADR-002: لا API خارجي في الـpipeline الأساسي).
  ثنائي خارجي منفصل بالضبط مثل ffmpeg (ADR-013)، يُستدعى عبر
  ``subprocess`` **بلا أي تبعية pip جديدة** (لا ``pytesseract``).
* **معطّل افتراضيًا** (`KeyframeConfig.enable_ocr`) — يضيف وقت معالجة
  ملموسًا لكل لقطة ويحتاج تثبيتًا يدويًا. تفعيل من الواجهة: خانة
  «استخراج نص الشاشة من الصور (OCR)» بجانب «تنظيف الصوت». عدم تثبيت
  Tesseract لا يوقف المعالجة إطلاقًا — فقط يترك نص الشاشة فارغًا، مع
  رسالة تشخيص واضحة في السجلّ، وفحص اختياري (لا يمنع التشغيل) في
  `tools/doctor.py`.
* **مكان التنفيذ:** داخل مرحلة KEYFRAMES الموجودة (لا مرحلة Stage
  جديدة) — بعد اختيار اللقطات مباشرة، قبل حفظ ``keyframes.json``. نتيجة
  ذلك تُستأنف تلقائيًا مع بقية المرحلة عبر آلية الاستئناف الحالية بلا
  أي تغيير عليها.
* **المسار حتى المستند:** `KeyframeMetadata.ocr_text` → `DocumentBlock.
  ocr_text` (نسخ في كل من `document/planner.py` ومسار إعادة الصياغة
  بالذكاء الاصطناعي `ai/rewriter.py`) → فقرة في `word_generator.py`،
  مقصوصة عند 400 حرف حتى لا تُغرق شريحة كثيفة النص المستندَ.
* `LANGUAGES = "ara+eng"`: شرائح كثيرة تخلط مصطلحات إنجليزية أو كودًا
  برمجيًا وسط نص عربي (نفس ملاحظة قاموس المصطلحات).

`tests/test_ocr.py`، `tests/test_ocr_pipeline_integration.py`، وإضافات
في `test_planner.py` و`test_document_quality.py`: استدعاء Tesseract
الفعلي، غياب الثنائي، صورة تالفة أو فارغة، الانتقال عبر السلسلة كاملة
حتى ظهور النص في الملف الناتج، وعدم إضافة أي فقرة حين لا نص OCR.

## 1.3.5 — 1 سبتمبر 2026 · رسالة واضحة لو faster-whisper غير مثبَّت

**العطل:** faster-whisper هو المحرّك الافتراضي الوحيد المعروض (منذ 1.3.2).
لو لم يكن مثبَّتًا، كان بناء الـ pipeline يُسرِّب ``ModuleNotFoundError``
خامًا حتى الواجهة — تتبّع بايثون تقني بلا سبب مفهوم ولا حل مقترح.

**العلاج:** ``core/pipeline.py::_build_transcriber`` يلتقط
``ModuleNotFoundError`` تحديدًا للمحرّك الافتراضي ويرفع
``ModelUnavailableError`` عربيًا: «faster-whisper غير مثبَّت. ثبّته عبر:
pip install faster-whisper». ``tests/test_engine_probe_is_import_free.py``
يحاكي غياب الحزمة في عملية نظيفة ويؤكّد نوع الاستثناء ومحتوى الرسالة.

## 1.3.4 — 1 سبتمبر 2026 · تشفير الأسرار محليًا

**العطل:** `hf_token` (رمز HuggingFace) و`api_key` (مزوّد الصياغة) كانا
يُحفظان نصًا صريحًا في `config.yaml` — أي نسخة احتياطية، مزامنة سحابية،
أو مشاركة غير مقصودة للملف تكشف المفتاح كاملًا وقابلًا للقراءة مباشرة.

**العلاج:** `utils/secret_store.py` — تشفير محلي بمفتاح مولَّد على
الجهاز (32 بايت عشوائية، ملف منفصل بصلاحيات 0600 بجوار `config.yaml`)،
بمكتبات المكتبة القياسية فقط (`hashlib`، `hmac`، `secrets`) بلا أي
تبعية جديدة في requirements — قرار مقصود بدل مكتبة `keyring` (التي كانت
ستغطي Windows Credential Manager / macOS Keychain / Linux Secret Service
بتبعية خارجية واحدة، لكن بصلاحيات نظام قد تحتاج إعدادًا إضافيًا على بعض
توزيعات Linux).

* البناء: تدفّق مفاتيح من HMAC-SHA256 على غرار CTR، موثَّق بنمط
  Encrypt-then-MAC (فك تشفير قيمة مُعبَث بها يُعيد فارغًا لا نصًا خاطئًا).
  هذا ليس AES ولا مبنيًّا على مكتبة تشفير مُدقَّقة — خط الدفاع الحقيقي هو
  صلاحيات ملف المفتاح، لا قوة الشيفرة. انظر تعليق الوحدة للحدود كاملة.
* `AppConfig.save()`/`load()` يشفّران/يفكّان عند حدود التسلسل (قاموس
  YAML) فقط — `config.whisper.hf_token` و`config.rewrite.api_key` في
  الذاكرة يبقيان نصًا صريحًا، فلا يتأثر أي كود آخر في المشروع.
* إعداد قديم (قيمة بلا بادئة `enc:v1:`) يُقرأ شفافًا بلا خطوة هجرة
  يدوية، ويُشفَّر تلقائيًا عند أول حفظ لاحق.
* نسخ `config.yaml` وحده (بلا `.secret.key` المرافق) إلى جهاز آخر لا
  يُسقط الإعداد كله — الحقل السرّي فقط يعود فارغًا (فشل مُغلَق لا صامت).

`tests/test_secret_store.py`: تشفير/فك تشفير، كشف العبث، مفتاح مفقود،
إعداد قديم نصّي، وتأكيد أن الملف على القرص لا يحتوي القيمة الحقيقية
إطلاقًا. `tests/test_regressions_round_6.py::test_hf_token_is_persisted_
with_the_config` يحرس الآن ضد الرجوع إلى التخزين الصريح بدل تأكيده.

## 1.3.3 — 1 سبتمبر 2026 · هوية المهمة

**العطل:** `job_dir_for` كان يبني مجلد المهمة من اسم الملف (`stem`) فقط.
`رياضيات/Lecture.mp4` و`فيزياء/Lecture.mp4` — بل وحتى `Lecture.mp4` و
`Lecture.mkv` — كانت تنتهي كلها إلى مجلد مهمة واحد. بما أن الاستئناف
والمعالجة الدفعية ودمج المهام كلها مبنية على هذا المعرّف، فمعالجة
الملف الثاني تُفسد نواتج الأول صامتًا: لا خطأ، لا تحذير.

**العلاج:**

* اسم مجلد المهمة صار يحمل بصمة مصدر قصيرة (`_source_fingerprint` في
  `core/pipeline.py`): من المسار المطلق + حجم الملف + وقت التعديل، بلا
  قراءة محتوى الفيديو نفسه. الشكل: `Lecture__a91f4c2d`.
* مهمة أُنشئت قبل هذا الإصلاح (اسم مجلدها بلا بصمة) تبقى قابلة
  للاستئناف من مكانها — لكن فقط إن كان `video_path` المخزَّن في
  `job_state.json` مطابقًا حرفيًا للملف الحالي (`_legacy_job_dir_matches`).
  تطابق تقريبي هنا هو بالضبط التصادم الذي نتجنّبه.
* البصمة ضجيج بصري للمستخدم، فلا تظهر له: `strip_source_fingerprint`
  (في `utils/timestamps.py`) تُزيلها من الاسم المعروض في لوحة المهام
  السابقة (`core/job_registry.py`) ومن عناوين المستندات المدموجة
  (`document/merge.py` عبر `humanize_title`).

`tests/test_job_identity.py`: يعيد إنتاج السيناريو المُبلَّغ بالضبط
(ملفان بنفس الاسم من مسارين مختلفين ⇒ مجلدان مختلفان)، ويؤكّد بقاء
الاستئناف يعمل للمهام القديمة، وأن البصمة لا تسرّب إلى أي واجهة عرض.

## 1.3.2 — 1 سبتمبر 2026 · سبب البطء الحقيقي

**العطل:** بعد تثبيت المحرّك الاختياري (‏Cohere، ويجرّ PyTorch) قفز زمن
معالجة **نفس الملف** من ~1:45 إلى أكثر من ثلاث ساعات — **دون أن يُختار
ذلك المحرّك أصلًا**.

**السبب:** فحص توفّر المحرّكات كان ينفّذ `__import__("torch")` و
`__import__("transformers")` **عند كل إقلاع**، لمجرد كتابة «(غير مثبّت)»
بجوار اسم في قائمة. و PyTorch يحمّل بيئة OpenMP خاصة به، بينما
CTranslate2 (محرّك faster-whisper) يستعمل OpenMP أيضًا. بيئتان في عملية
واحدة تتنازعان الأنويةَ، فيتضاعف زمن التفريغ على المعالج.

**العلاج — ثلاث طبقات:**

* الفحص صار `importlib.util.find_spec`: يجيب على السؤال نفسه (أمثبَّتة
  الحزمة؟) **بلا تحميل أي شيء في العملية**. وإصدار `transformers` يُقرأ من
  بيانات التوزيعة لا من استيرادها.
* المحرّكات الاختيارية **لا تُعرض ولا تُفحص افتراضيًا**. `ENGINE_ORDER`
  صار `("faster-whisper",)`، وما عداها خلف `whisper.show_optional_engines`.
  ولا تُعرض قائمة المحرّك أصلًا حين لا يوجد خيار فعلي.
* حزام أمان: `app.py` يضبط `OMP_NUM_THREADS` قبل أي استيراد حسابي،
  و`transcriber` يسجّل تحذيرًا صريحًا إن وجد PyTorch محمَّلًا في العملية —
  فلا يعود العطل «بطء بلا سبب» في المرة القادمة.

`tests/test_engine_probe_is_import_free.py` يشغّل الفحص في **عملية نظيفة**
ويؤكّد أن `torch` و`transformers` لم يدخلا `sys.modules` — وهو القياس
الوحيد الصحيح، إذ قد تكون حزمة الاختبارات استوردت شيئًا قبله.

> المحرّك الاختياري **لم يُحذف**: يبقى متاحًا بالاسم من الإعداد أو سطر
> الأوامر لمن يملك كرت رسومات. المحذوف ظهورُه الافتراضي وفحصُه التلقائي.

## 1.3.1 — 1 سبتمبر 2026 · إصلاح التخطيط والسرعة

* **انهيار تخطيط الواجهة.** مع نمو الواجهة تجاوز مجموعُ الارتفاعات الدنيا
  ارتفاعَ الشاشة، فضغط Qt المجموعاتِ فوق بعضها ورُسمت الصفوف على بعضها —
  واجهة غير مقروءة بلا أي خطأ في السجلّ. العلاج بنيوي لا تجميلي:

  * كل المحتوى داخل `QScrollArea`، فالانهيار صار مستحيلًا مهما أُضيف لاحقًا.
  * صفوف المزوّد والمفتاح ومساحة العمل تُخفى لا تُعطَّل — المعطّل يشغل
    ارتفاعه كاملًا ويدفع بقية الواجهة خارج الشاشة.
  * مجلد الحفظ صار صفًّا داخل القسم الأول لا مجموعة مستقلة (~90 بكسل).
  * السجلّ بارتفاع محدّد لا متمدّد.
  * `tests/test_ui_layout.py`: يبني الواجهة بلا شاشة عند أربعة ارتفاعات
    ويؤكّد ألّا تتقاطع المجموعات. الفحص النحوي لا يمسك هذا العطل إطلاقًا.

* **عدد خيوط المعالج كان ثابتًا على 4.** يترك نصف الأداء على جهاز
  بثمانية أنوية، ويُثقِل جهازًا بنواتين بتبديل السياق. صار يُكتشف تلقائيًا
  (`cpu_threads: 0`) مع ترك نواة للنظام حتى لا يتجمّد الجهاز أثناء ساعات
  التفريغ.

* **اختيار فكّ التشفير في الواجهة.** «دقيق (بحث شعاعي)» أو «سريع (جشع)» —
  الأخير أسرع بنحو مرّتين على المعالج بكلفة دقة متواضعة. على محاضرة 82
  دقيقة الفارق ساعات لا دقائق. والتقدير المعروض يعكس الاختيار.

## 1.3.0 — 1 سبتمبر 2026 · جولة الميزات

سبع ميزات طلبها الاستخدام الفعلي، كلها مبنية على ما كان المعمار يوفّره
أصلًا ولم يكن مستثمَرًا.

**نتيجة الاختبارات:** 307 اختبارًا ناجحًا · `ruff` نظيف · زمن الحزمة نزل
من 233 إلى 142 ثانية بفضل إصلاح منتقي الصور.

> ملاحظة: `acceptance_report.json` المرفق وُلِّد قبل سطر واحد أُصلح بعده في
> `tools/run_acceptance.py` (لم يكن يمرّر `allow_audio_only` لحالة الصوت،
> فكانت تُحسب فشلًا: 699/700). أعِد توليده بـ `python tools/run_acceptance.py`
> بعد `python tools/make_test_corpus.py` لتحصل على الأرقام السارية.

### قصّ المقطع (من / إلى)

حقلان في الواجهة و`--from`/`--to` في سطر الأوامر، بصيغة ثوانٍ أو `MM:SS`
أو `HH:MM:SS`. تجربة إعداد على محاضرة ثلاث ساعات كانت تكلّف ساعات انتظار؛
صارت خمس دقائق.

* **التوقيتات تبقى مطلقة** بزمن المصدر الأصلي. الصوت المستخرج يبدأ من
  الصفر، فتُزاح كل توقيتات التفريغ — بحراسة تمنع الإزاحة المزدوجة، لأن
  الكلمة الواحدة مشتركة بين `segment.words` والقائمة العليا ونسخُ pydantic
  العميق يحفظ هذا الاشتراك (أمسكه اختبار انحدار).
* **نطاق مختلف ⇒ مجلد مهمة مختلف.** بلا هذا التمييز تُفسد التجربةُ نواتجَ
  المعالجة الكاملة أو تُستأنف فوقها.
* كاشف المشاهد يقفز إلى البداية ويتوقّف عند النهاية بدل مسح الملف كله.

### قاموس مصطلحات المادة

مربّع نص في الواجهة يُغذّي `initial_prompt` الموجود أصلًا. تقرير القبول
حدّد أن الأخطاء شبه محصورة في أسماء الأعلام المنقولة — وهذا أرخص علاج لها.
مقصوص بحدّ محافظ حتى لا يزيح الموجّه الأساسي (حدّ Whisper 224 رمزًا).

### تنظيف الصوت قبل التفريغ

`highpass=80` لقطع الهدير، و`afftdn` لخفض الضوضاء، و`loudnorm` لتوحيد
المستوى — بمرشّحات ffmpeg بلا اعتمادية جديدة. تقرير القبول: «صوت نظيف —
الأثر الأكبر منفردًا» على الدقة.

### وضع «تفريغ فقط»

يقف عند التفريغ ويُخرج `txt` و`md` و`srt` و`vtt` بلا بناء مستند. الفقرات
تُدمج عند فجوات الصمت وحدود الطول، لا كل مقطع سطرًا.

### المعالجة الدفعية لمجلد كامل

`core/batch.py` وزر «مجلد كامل…» و`--batch`. **فشل ملف لا يوقف الدفعة** —
من يشغّل مقرّرًا ليلًا يجد صباحًا كل ما نجح وتقريرًا بما فشل. الإلغاء وحده
يوقف الطابور.

### لوحة المهام السابقة

`core/job_registry.py` و`ui/jobs_dialog.py`: جدول بكل المهام وحالتها
ومرحلتها وتقدّمها، مع فتح المستند والمجلد وإعادة البناء والاستئناف.
الاستئناف كان مبنيًّا ومختبَرًا منذ 1.2 وغير مرئي — يصل إليه المستخدم
بالصدفة فقط، بإعادة اختيار نفس الملف.

### دمج عدة مهام في مستند واحد

`document/merge.py`: كل جزء قسمٌ رئيسي وأقسامه تحته، بفهرس واحد وترقيم
أشكال متصل. المصيدة الجوهرية كانت **تصادم المعرّفات** — كل مهمة ترقّم
مقاطعها من 1 — فتُزاح معرّفات كل جزء بقاعدة مستقلة قبل الدمج، وإلا أسقطت
بوابة `assert_lossless` الدمجَ محقّةً.


## 1.2.0 — 1 سبتمبر 2026 · جولة الإصلاحات والتشديد

**نتيجة القبول بعد الجولة:** 661/661 فحصًا = **100.00%**
(‏253 اختبارًا · 30/30 في مصفوفة التوافق · 378/378 بوابة جودة مستند)،
مقابل 638 فحصًا و230 اختبارًا قبلها. `ruff check` نظيف بالكامل.

جولة نتجت عن تدقيق كامل للشجرة. كل بند أدناه له اختبار انحدار في
`tests/test_regressions_round_6.py` مكتوب ليفشل على الشيفرة قبل الإصلاح.

### أعطال مُصلَحة

* **تنزيل صامت للنموذج بلا إذن (خرق ADR-014).** `ModelManager.is_available`
  كان يطابق اسم النموذج مطابقةً **جزئية**، و`large-v3` مقطع فرعي من
  `large-v3-turbo`. النتيجة: مستخدم لديه turbo فقط ويختار `large-v3` ترى
  الواجهة أنه «✓ مثبّت»، فتمرّ `ensure_available` بلا سؤال، ثم ينزّل
  faster-whisper 3.1 جيجابايت من الإنترنت صامتًا. المطابقة صارت دقيقة،
  وأُضيف حاجز ثانٍ: `local_files_only` على المحرّك لا يُرفع إلا بإذن صريح.

* **كتابة في مجلد فيديو المستخدم (خرق §29).** `resolve_rotation_plan` كان
  يكتب إطارًا مرجعيًا مؤقتًا بجوار ملف المصدر — ويفشل على مجلد للقراءة فقط
  أو وسيط شبكي. صار يكتب في مجلد المهمة المؤقت، وكذلك إطار الفحص في
  منتقي الصور الذي كان يتسرّب إلى مجلد الصور الناتج.

* **مجلد `logs/` يُنشأ وقت الاستيراد.** آخر سطر في `utils/logger.py` كان
  ينفّذ `setup_logger(Path("logs"))`، فينشئ مجلدًا ويفتح ملفًا في **مجلد
  العمل الحالي** أيًا كان — ويفشل في تثبيت مُغلَّف تحت `Program Files`.
  الاستيراد الآن يهيّئ `stderr` فقط، وملف السجلّ لا يُفتح إلا بطلب صريح.

* **محرّكات التفريغ (ADR-017) غير موصولة.** السجلّ والواجهة المجرّدة
  ومحرّك Cohere كانت مكتوبة ومختبَرة، لكن `build_engine` لا يُستدعى إلا من
  الاختبارات وأداة القياس. أُضيف حقل `whisper.engine` إلى الإعداد، وقائمة
  في الواجهة، وخيار `--engine` في سطر الأوامر، مع سقوط آمن إلى الافتراضي.

* **تجميد الواجهة حتى 30 ثانية.** اختبار مفتاح المزوّد كان يُنفَّذ داخل
  معالج الزر مع `processEvents()` — خرق ADR-001 والمواصفة §15. انتقل إلى
  `ProviderTestWorker` على خيط خلفي.

* **ضياع إعدادات المستخدم.** الإعداد كان يُحفظ في موضع واحد فقط (بعد نجاح
  اختبار المفتاح)، فتضيع كل الخيارات عند الإغلاق. صار يُحفظ في `closeEvent`.

* **خطأ مزوّد واحد يُهدر كل الدفعات.** `RewriteUnavailableError` كان يُعاد
  رفعه من الدفعة إلى الـ pipeline، فتحديدُ معدّل (429) عند الدفعة الثانية
  عشرة يرمي إحدى عشرة دفعة مدفوعة الثمن. الفشل صار معزولًا عند حدود الدفعة،
  مع تمييز الخطأ العابر من النهائي وإعادة محاولة بتراجع أسّي.

* **إعداد تالف يُتجاهَل بصمت.** `AppConfig.load` كان يبتلع كل استثناء ويعود
  للافتراضيات دون رسالة. صار يحتفظ بالسبب في `load_error` ويسجّله.

* **`validate_readable` مكتوبة ولا تُستدعى.** ملف يقرأ ffprobe رأسه ويعجز
  عن فك إطاراته كان يمرّ حتى مرحلة الصور. صار يُفحص قبل بدء المعالجة
  (المواصفة §22).

* **عقد السجلّات (§25) نصف منفَّذ.** `JOB=` و`STAGE=` كانتا دائمًا `-`.
  أُضيف `bind_job` وتُملآن من `JobManager`، مع تسجيل مدة كل مرحلة وعدد
  المحاولات عند الفشل.

* **تعبير نمطي مكسور.** `_SENTENCE_END` كتب `\\s` داخل سلسلة خام، فكان
  أحد بديلَي التعبير ميتًا.

* **فشل محرّك اختياري أثناء التنفيذ كان يُسقط المهمة كلها.** ‏`is_available`
  تفحص وجود الحزم فقط؛ محرّك Cohere ينجح فيها ثم يفشل عند تحميل أوزانه
  (نموذج مقيّد الوصول على HuggingFace، أو ذاكرة غير كافية). النتيجة كانت
  فشلًا عند 20% بعد نجاح كل المراحل السابقة. الآن يسقط إلى Whisper ويكمل —
  نفس قاعدة السقوط من CUDA إلى المعالج (تغطية التحميل **والتنفيذ**).
  والإلغاء يبقى إلغاءً لا يُعامَل معاملة الفشل.

* **رمز HuggingFace يُمرَّر صراحةً** من `HF_TOKEN` أو `HUGGING_FACE_HUB_TOKEN`
  بدل الاعتماد على اكتشاف `huggingface_hub` الذي يختلف بين إصداراتها،
  والرسالة تفرّق بين «لا رمز» و«رمز موجود والوصول مرفوض».

* **تقدير المدة صار صادقًا لكل محرّك.** الأرقام المقاسة تخصّ Whisper وحده،
  وكانت تُعرض لأي محرّك — أي توقّع مختلق لمحرّك قد يستغرق أضعافها.

* **منتقي الصور كان يبحث عشوائيًا داخل الحلقة (العطل B7 مجددًا).**
  ‏`cap.set(CAP_PROP_POS_FRAMES)` لكل قراءة يفرض على OpenCV فكّ الترميز من
  أقرب إطار مفتاحي: خمسة مرشّحين × ثلاث قراءات = **خمس عشرة قفزة لكل
  مشهد**. على محاضرة بمئات المشاهد تصير المرحلة أبطأ من التفريغ نفسه.
  حزمة التصحيحات أدانت هذا النمط وأصلحته في كاشف المشاهد **دون** المنتقي.
  الآن: مرجع نهاية المشهد يُقرأ مرة واحدة لكل مشهد لا لكل مرشّح،
  وإطار الاستقرار القريب يُقرأ بالتقدّم التسلسلي (`grab`) بدل قفزة ثانية.
  **قياس: 35 قفزة بدل ~80 لخمسة مشاهد (‏‑56%)**، بطوابع زمنية مطابقة
  تمامًا، وزمن الحزمة الاختبارية نزل من 233 إلى 174 ثانية.

* **شريط التقدّم كان ساكنًا طوال استخراج الصور.** إشارة واحدة قبل الحلقة
  فقط — فتبدو المرحلة متجمّدة ويُلغي المستخدم عملًا سليمًا. صار يعرض
  «المشهد س من ص» ثم «حفظ الصور».

### ميزات جديدة

* **ملفات ترجمة SRT و VTT** بجوار المستند (`document/subtitles.py`). البيانات
  كانت مدفوعة الثمن وغير مستعملة: التفريغ يعمل بـ `word_timestamps=True`
  ولم تكن أي وحدة تقرأ `words`. المقاطع الطويلة تُقسَّم عند حدود كلمات حقيقية.

* **إعادة بناء المستند بلا إعادة معالجة** — زر في الواجهة و`--rebuild` في
  سطر الأوامر. تغيير الخط أو الشعار أو حجم الصور كان يستلزم إعادة المهمة
  كاملة (ساعات)؛ صار ثوانٍ من `plan.json` المحفوظ.

* **`tools/calibrate_scenes.py`** — الأداة التي يشير إليها ADR-011 وتقرير
  القبول ولم تكن موجودة. تعرض توزيع الدرجات وعدد المشاهد عند قيم `k`
  مختلفة، وتوصي بقيمة من «الهضبة المستقرة»، و`--apply` يكتبها في الإعداد.

* **فهرس الأشكال** في المستند، مبنيًّا على حقول `SEQ` حقيقية بدل أرقام
  نصّية — فيعيد Word الترقيم تلقائيًا عند حذف صورة.

* **اختيار مجلد الحفظ من الواجهة.** كان مثبّتًا في الإعداد بلا أي سبيل
  لتغييره.

* **تفريغ الملفات الصوتية** — زر مستقل يقبل أي امتداد يفكّه ffmpeg
  (‏mp3، m4a، wav، flac، opus…) وينتج مستند Word منسّقًا كاملًا بلا لقطات.
  العقد يحمل `has_video`، والـ pipeline يتخطّى الدوران وكشف المشاهد
  واستخراج الصور، وجدول الغلاف يعرض «نوع المصدر» بدل «الدقة 0×0».
  القبول **صريح** (`allow_audio_only`): ملف صوتي يُختار بالخطأ في منتقي
  الفيديو يبقى خطأً واضحًا. متاح أيضًا بـ `--audio` في سطر الأوامر.

* **`make_test_corpus.py`: مدخل مكرّر ومهمل لحالة الدوران.** كان يولّد
  `rotated_90.mp4` مرتين — مرة بـ `-metadata rotate=90` (مهمل ولا يكتب
  شيئًا في ffmpeg 6) ومرة بـ `-display_rotation`. النتيجة: مدخل مكرّر في
  `manifest.json` تُعالَج معه الحالة مرتين، وملف بلا بيانات دوران إن
  توقّف التوليد بينهما فيفشل اختبار الدوران برسالة غامضة.

* **تشخيص المحرّكات بدل «غير مثبّت».** ‏`diagnose()` على واجهة المحرّكات
  يعيد السبب الفعلي — اسم الحزمة الناقصة، أو نص خطأ الاستيراد (‏`OSError`
  من DLL ناقصة على ويندوز كان يتسرّب لأن الفحص يلتقط `ImportError` وحدها)،
  أو أن `transformers` أقدم من 5.4. يُعرض في الواجهة وفي `tools/doctor.py`،
  ومعه **مسار المفسّر** — لأن أشهر سبب هو أن التثبيت أصاب Python آخر.

* **`tools/install_engine.py`** — يثبّت تبعيات المحرّك الاختياري في
  `sys.executable` حصرًا، فيستحيل أن تصل الحزم إلى مفسّر خاطئ. يكتشف كرت
  NVIDIA ويختار بناء PyTorch المناسب (‏320 ميجابايت للمعالج بدل 2.5
  جيجابايت)، ثم يتحقّق بنفس منطق البرنامج. ومعه ملف
  `تثبيت_محرك_Cohere.bat` للتشغيل بنقرة مزدوجة.

* **سطر أوامر مكتمل**: `--config` و`--engine` و`--rewrite` و`--rebuild`.

### البنية التحتية

* `.gitignore` و`pyproject.toml` (‏pytest و ruff و mypy) و`CHANGELOG.md`.
* سير عمل CI يشغّل `tools/run_acceptance.py` — وهو مصمَّم لذلك برمز خروجه
  منذ البداية ولم يكن هناك ما يشغّله — على Linux و Windows، و3.10 و3.12.
* سير عمل بناء يشغّل PyInstaller على Windows عند كل وسم إصدار.
* **`build.spec`** — كان مذكورًا في حزمة التصحيحات وتقرير القبول وغير موجود.
* سقوف على الإصدارات الكبرى في `requirements.txt`، و`tools/lock_requirements.py`
  لتوليد قفل حقيقي على بيئة البناء نفسها.

### حذف

كود ميّت من إعادات هيكلة سابقة: `TimelineMatchResult` و`DocumentItem`،
ودوال العزل الاتجاهي المهجورة (`isolate_ltr`، `isolate_mixed`،
`auto_isolate`، `LRI`، `PDI`)، ومتغيّرات وحلقات ميتة في المخطِّط وفاحص
البيئة ومُدقِّق القبول.

> حزمة `matching/` بقيت فارغة (انتقل عملها إلى `document/planner.py`).
> احذفها يدويًا — تعذّر حذف الملفات في بيئة هذه الجولة.

## 1.1.0 وما قبلها

انظر `FIXES_ROUND_5.md` و`Corrections_Pack_v1.2.md` و`Acceptance_Report_v1.0.md`.

## 1.4.0 — Runtime & Release Hardening (R3)

- إضافة تصنيف أخطاء قابل للمعالجة آليًا (`error_code` / `category`).
- جعل إنشاء DOCX ذريًا مع فحص سلامة OOXML قبل نشر الملف النهائي.
- إضافة فحص مساحة القرص وصلاحية الكتابة إلى Doctor.
- إضافة `tools/build_smoke.py` للتحقق من ملفات حزمة PyInstaller وDOCX.
- جعل `FasterWhisperEngine` قابلًا للوصف والفحص دون كسر الإقلاع عند غياب faster-whisper.
- تحسين رسالة نقص faster-whisper لتتضمن أمر التثبيت.
