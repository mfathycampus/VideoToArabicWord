# إعادة الصياغة بالذكاء الاصطناعي

تحوّل مخرج Whisper (كلام منطوق: جُمل ناقصة، تكرار، «يعني» و«طيب») إلى
نص وثيقة مكتوب، مع عناوين أقسام وملخّص تنفيذي.

**معطّلة افتراضيًا.** لا تعمل إلا بتفعيلك لها من الواجهة.

---

## الإعداد بمفتاح Claude

### ٠ · أنشئ المفتاح بالشكل الصحيح

في Claude Console ← **API keys** ← **Create key**:

| الحقل | الاختيار الصحيح | لماذا |
|---|---|---|
| **Name** | اسم واضح مثل `video-to-word` | لتعرف أيّ مفتاح تُلغي لاحقًا |
| **Expires** | أطول مدة متاحة | ٣٠ يومًا تعني توقّف البرنامج بعد شهر بخطأ 401 |
| **Scope** | **اختر مساحة عمل محددة** ← الأهم | يجعله مفتاح مساحة عمل فلا يحتاج ترويسة إضافية |

**الخانة الحاسمة هي Scope.** تركها على `Same as linked account (default)`
يُنشئ مفتاحًا **مرتبطًا بهويتك**، نطاقه المنظمة كلها، فيطلب معه
`anthropic-workspace-id` ويرفض الطلب بـ 400 بدونه — وهو الخطأ الذي
يظهر لك.

اختيار مساحة عمل محددة يُنشئ مفتاحًا مرتبطًا بها وحدها، فيعمل مباشرةً
بلا أي إعداد إضافي.

> إن كنت مضطرًا لمفتاح هوية، فالبرنامج يدعمه: ضع معرّف مساحة العمل
> (`wrkspc_…`) في حقل «معرّف مساحة العمل».

**للاستخدام طويل الأمد:** أنشئ **Service account** (من القائمة الجانبية)
ثم مفتاحًا تابعًا له. لا يرتبط بشخص، ولا يتعطّل إن غادر أحد المنظمة.

---

### ١ · ضع المفتاح — طريقتان

**الأسهل: ملف الإعداد** (يعمل فورًا بلا إعادة تشغيل)

أنشئ الملف `C:\Users\Admin\.config\video_ai_doc\config.yaml`:

```yaml
rewrite:
  enabled: true
  provider: anthropic
  api_key: "sk-ant-api03-..."
  model: claude-sonnet-5
```

> ⚠ المفتاح يُحفظ نصًا صريحًا — استخدم هذه الطريقة على جهازك الشخصي فقط.

**البديل: متغيّر بيئة**

```powershell
setx ANTHROPIC_API_KEY "sk-ant-api03-..."
```

> `setx` لا يؤثر على البرامج المفتوحة. **أغلق PowerShell و VS Code
> وافتحهما من جديد** — وإلا سيظل البرنامج يقول «غير مهيّأ» رغم ضبطك
> للمفتاح. هذا أشهر التباس في الإعداد.

للتأكد:

```powershell
echo $env:ANTHROPIC_API_KEY
```

### ١ب · معرّف مساحة العمل (عند الحاجة فقط)

إن ظهر هذا الخطأ:

```
anthropic-workspace-id is required when authenticating
with an identity-linked API key
```

فمفتاحك **مرتبط بهوية** لا بمساحة عمل. الحل أحد أمرين:

**(أ) أضف معرّف مساحة العمل** — افتح Claude Console ← Settings ←
Workspaces، انسخ المعرّف (يبدأ بـ `wrkspc_`)، وألصقه في حقل **«معرّف
مساحة العمل»** في الواجهة.

**(ب) أنشئ مفتاحًا تابعًا لمساحة عمل محددة** بدل مفتاح الهوية — لا
يحتاج معرّفًا إطلاقًا، وهو الأبسط.

---

## ⚠ أمان المفتاح

* **لا تشارك المفتاح في صورة أو رسالة.** من يراه يستطيع استخدام رصيدك.
* إن ظهر مفتاحك لأي شخص: افتح Console ← API keys ← احذفه فورًا
  (⋮ ← Delete) وأنشئ غيره. لا يمكن «إخفاؤه» بعد ظهوره.
* المفتاح يُحفظ نصًا صريحًا في `config.yaml` — على جهازك الشخصي فقط.

---

### ٢ · فعّل الخيار

شغّل `python app.py`، وفي القسم **«٣ · صياغة النص»**:

* ضع علامة على مربّع إعادة الصياغة
* اختر **Claude API** (الخيار الأول)
* سيظهر أسفله: `جاهز ✓ · النموذج: claude-sonnet-5`

عند الضغط على «ابدأ المعالجة» تظهر نافذة تأكيد قبل أي إرسال.

---

## ما يُرسل وما لا يُرسل

| | |
|---|---|
| ✅ يُرسل | نص التفريغ فقط |
| ❌ لا يُرسل أبدًا | الفيديو · الصوت · الصور · اسم الملف |

نص التفريغ الخام يبقى محفوظًا على جهازك في `transcription.json` مهما حدث.

---

## الضمانات المفروضة برمجيًا

هذه ليست وعودًا في التوثيق — كلٌّ منها مغطّى باختبار:

* **النص الخام لا يُمسّ** (ADR-005) — الصياغة طبقة موازية في `plan.json`
* **لا ضياع ولا تكرار** (ADR-010) — كل مقطع يظهر مرة واحدة بالضبط،
  ويُفحص في `assert_lossless` بعد الصياغة كما قبلها
* **التتبّع** — كل قسم يحمل `segment_ids` مصدره
* **فشل الصياغة لا يُفشل المهمة** — سقوط تلقائي إلى النص الخام
* **فشل دفعة واحدة** يستخدم نصها الخام بدل إسقاطها
* **النموذج مُوجَّه صراحةً**: لا يضيف معلومة، ولا يحذف معنى، يحذف الحشو فقط

---

## البديل المحلي (بلا إنترنت)

```bash
ollama pull qwen2.5:7b-instruct
```

ثم اختر **Ollama** من القائمة. النص لا يغادر جهازك إطلاقًا، لكن جودة
الصياغة العربية أقل من Claude.

---

## تغيير النموذج

في `~/.config/video_ai_doc/config.yaml`:

```yaml
rewrite:
  enabled: true
  provider: anthropic
  model: claude-sonnet-5     # أو claude-opus-5 لصياغة أدق وأبطأ
  batch_chars: 3500          # حروف الدفعة الواحدة — أكبر = طلبات أقل
```

---

## الكلفة التقريبية

الدفعة الافتراضية 3500 حرف. محاضرة ساعة (~45 ألف حرف عربي) ≈ 13 طلبًا
+ طلب للملخّص. ارفع `batch_chars` لتقليل عدد الطلبات.

---

## حلّ المشاكل

| الرسالة | المعنى |
|---|---|
| `غير مهيّأ ✗` | المفتاح غير مضبوط، أو لم تُعِد فتح البرنامج بعد `setx` |
| `رفضت Anthropic المفتاح (401)` | المفتاح خاطئ أو منتهٍ |
| `تجاوزت حد الاستخدام (429)` | انتظر قليلًا أو استخدم Ollama |
| `النموذج غير متاح لحسابك (404)` | غيّر `model` في ملف الإعداد |

في كل الحالات: **المعالجة تكمل بالنص الخام** ولا تفشل المهمة.


---

## لماذا قد لا تُطبَّق الصياغة رغم تفعيلها

**السبب الأشهر: مهمة مكتملة من تشغيل سابق.**

النظام يحفظ كل مرحلة ويستأنف منها. لو شغّلت الفيديو أولًا **بلا صياغة**
فاكتملت المهمة، ثم فعّلت الصياغة وأعدت التشغيل على **نفس الفيديو**،
كان الاستئناف يتخطّى بناء الوثيقة لأنها «مكتملة» — فيخرج المستند خامًا
دون أي رسالة خطأ.

أُصلح هذا: الخطة تُقارَن الآن بإعدادك الحالي، وتُعاد إن اختلف. سترى في
السجل:

```
تغيّرت طريقة بناء الوثيقة (timeline ← ai:anthropic) — إعادة البناء.
```

المراحل الثقيلة (التفريغ، كشف المشاهد، الصور) **لا تُعاد** — يُعاد بناء
الوثيقة فقط، وهو ثوانٍ معدودة.

---

## ما تغيّره الصياغة وما لا تغيّره

**تُصلح:**

* عناوين أقسام حقيقية بدل «القسم الأول — من 00:00:00»
* ملخّص تنفيذي ونقاط رئيسية على الغلاف
* نثر مكتوب بدل كلام منطوق: حذف «يعني» و«طيب» والتكرار والتلعثم
* ترقيم وفقرات منطقية

**لا تُصلح:**

* **كلمة سُمعت خطأً تبقى خطأً.** إن فرّغ Whisper «مني الطالب من فاصل
  بفاصل» فالنموذج لا يعرف الصواب — سيحرّرها لغويًا لا معنويًا.

علاج ذلك في التفريغ نفسه لا في الصياغة: صوت أنظف، أو `initial_prompt`
بمصطلحات مادتك:

```yaml
whisper:
  initial_prompt: "شرح نظام معارف: تسجيل الطلاب، الجدول الدراسي، حذف المواد، الفصول."
```

هذا يرفع دقة أسماء الأعلام والمصطلحات المتخصصة بوضوح.
