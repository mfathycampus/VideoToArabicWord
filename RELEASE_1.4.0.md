# VideoToArabicWord 1.4.0 — Engineering Hardening

## ما تم تنفيذه

- توحيد إصدار التطبيق في `version.py` وربطه بإصدار الحزمة `pyproject.toml`.
- إضافة بصمة مصدر أقوى تعتمد على المسار المطلق، الحجم، وقت التعديل بالنانوثانية، وعينات من بداية ونهاية الملف.
- تسجيل `source_fingerprint` داخل `job_state.json` لمنع استئناف نتائج تخص ملفًا تغيّر فعليًا.
- إضافة `processing_fingerprint` لكل Artifact مهم؛ تغيير إعدادات المعالجة يجعل الناتج القديم غير قابل لإعادة الاستخدام تلقائيًا.
- جعل التحقق من Artifact يفحص الوجود + checksum + بصمة المعالجة عند توفرها.
- دعم ترقية Job State من schema 1.3 إلى 1.4 بدل إسقاط كل المهام القديمة المدعومة.
- تقوية ربط مراحل التفريغ والمشاهد واللقطات والخطة والمستند بإعداداتها ومدخلاتها.
- إضافة اختبارات انحدار خاصة بتغيّر المصدر وتغيّر إعدادات المعالجة.

## ملاحظة الاختبارات

الحزمة المرفقة لا تحتوي `testdata/corpus/` الذي تتوقعه بعض اختبارات E2E/acceptance، لذلك لا ينبغي تفسير إخفاقات تلك الاختبارات في بيئة الفحص الحالية على أنها فشل في هذه التعديلات. الاختبارات المستقلة عن corpus اجتازت بنجاح.
