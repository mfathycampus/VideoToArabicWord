# التشغيل على جهاز جديد

## ١ · المتطلبات

* **Python 3.10 أو أحدث** — من [python.org](https://www.python.org/downloads/)،
  وفعّل خيار **Add Python to PATH** أثناء التثبيت.
* **ffmpeg و ffprobe** — يُجلبان بأمر واحد في الخطوة ٣.
  (‏`bin/` غير مضمَّن في هذه الحزمة لأن حجمه ~196 ميجابايت.)

## ٢ · التثبيت

في PowerShell، داخل مجلد المشروع:

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -U pip
python -m pip install -r requirements.txt
```

> إن رفض PowerShell تفعيل البيئة:
> `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`

## ٣ · ffmpeg والتحقق

```powershell
python tools\setup_ffmpeg.py
python tools\doctor.py
```

‏`doctor.py` يفحص كل شيء ويطبع أوامر الإصلاح جاهزة للنسخ إن نقص شيء،
ويعرض حالة محرّكات التفريغ وسبب عدم جاهزية أيٍّ منها.

## ٤ · التشغيل

```powershell
python app.py
```

عند أول تشغيل سيطلب إذنك لتنزيل نموذج التفريغ (‏`large-v3-turbo` ≈ 1.7GB،
مرة واحدة). بعدها يعمل البرنامج **دون اتصال بالإنترنت**.

## ٥ · أول تجربة موصى بها

لا تبدأ بمحاضرة كاملة. في «خيارات متقدّمة» اضبط **من `00:00` إلى `02:00`**
وشغّل — تحصل على مستند خلال دقائق وترى الشكل النهائي قبل أن تلتزم بساعات.

## اختياري

| الميزة | الأمر |
|---|---|
| محرّك Cohere للّهجات (يحتاج كرت رسومات) | انقر `تثبيت_محرك_Cohere.bat` |
| إعادة الصياغة بالذكاء الاصطناعي | القسم ٣ في الواجهة + مفتاح Claude |
| معايرة كشف المشاهد على مادتك | `python tools\calibrate_scenes.py محاضراتي\ --apply` |
| قفل الإصدارات لإعادة إنتاج البيئة | `python tools\lock_requirements.py` |

## نقل الإعدادات من جهازك الحالي

ملف الإعداد في:

```
C:\Users\<اسمك>\.config\video_ai_doc\config.yaml
```

انسخه إلى نفس المسار على الجهاز الجديد لتنتقل معه كل خياراتك.
**⚠ يحوي مفاتيح الوصول نصًّا صريحًا** — لا تشاركه.
